# ARGUS-RESEARCH-GOV-001 Release Report

## Classification

`IMPLEMENTED_PENDING_MERGE`

Canonical baseline is `ea056155182351be70bb03d23841aca55c6118ae`.
Implementation is isolated on
`codex/ARGUS-RESEARCH-GOV-001-experiment-registry`. Nothing is merged,
installed, activated, scheduled, or repinned.

## Delivered

- Contract version 1 immutable experiment, amendment, variant, feature,
  sample/dataset, partition, holdout-access, result, invalidation, and
  model-health records.
- Exact prospective/retrospective and exploratory/confirmatory identities.
- Full parameter-search classification and visible multiple-comparison counts.
- Permanent negative, null, failed, invalid, abandoned, and contaminated
  evidence.
- Caller-rooted canonical write-once persistence with idempotency, restart
  validation, tamper rejection, and partial-write rejection.
- Pure read-only summaries with no best-model or profitability selector.
- Exact static specialist fixtures, inactive STAT-DATA compatibility, and
  RESEARCH-DATA limitation metadata.

## Proof

- Focused research-governance suite: 33 tests passed.
- Adjacent integrated SETUP-002 suite: 28 tests passed.
- STAT-DATA runtime test module is intentionally absent from canonical
  `ea056155` because STAT-DATA is an unmerged sibling; exact static identity,
  inactive zero-observation status, and no-runtime-import compatibility are
  covered by focused tests instead.
- Full Python discovery: 2,046 tests passed in 292.087 seconds.
- `compileall` and direct module compilation passed.
- Static capability scan found only Python standard-library imports and no
  provider, network, account, broker, order, service, scheduler, Engine Host,
  SETUP-002, or STAT-DATA runtime import.
- Registry tests use temporary caller roots. No production registry exists and
  no generated registry report is tracked.

The full suite created ignored `_test-*` scratch directories only inside the
disposable feature worktree. An attempted local cleanup was blocked by the
host destructive-operation policy, so they remain untracked and isolated;
canonical production storage was not touched.

## Nonmutation

- Canonical checkout remains clean at `ea056155`; local `master` equals
  `origin/master`.
- Installed automation manifest SHA-256 remains
  `8C211729AE78DCDAEF6BC16497E9F4C797B7FDD87B34F2AB9583FCD45AD6A329`.
- The four Aug. 17 opening, Paper, and SETUP-002 jobs remain pinned to exact
  canonical `ea056155` with their existing dependencies and timeouts.
- No scanning, scoring, ranking, TradePlan, Risk Governor, allocation, Paper,
  Shadow, broker/order, provider, credential, production data, database,
  service, scheduler, Engine Host, or UI file changed.

## Remaining Work

A later deliberate merge may integrate the dormant contracts. Separate tasks
are required to choose a production research root, register an actual
experiment prospectively, execute analysis, record real results, evaluate
model health, or promote any specialist. None is authorized here.
