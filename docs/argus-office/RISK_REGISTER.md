# Risk Register

| ID | Risk | Area | Severity | Mitigation | Status |
| --- | --- | --- | --- | --- | --- |
| R-001 | Multiagent work creates fragmented recommendations. | Operations | Medium | Codex Orchestrator produces one consolidated CEO report. | Open |
| R-002 | Analysis agents accidentally modify code. | Governance | High | Recommendation-only agents are read-only by default; Builder is the only normal code-writing agent. | Open |
| R-003 | Protected trading or replay semantics change outside the authorized scope. | Product trust | High | Protected areas require exact task scope, Hard Chew proof, and interruption before semantic expansion. | Open |
| R-004 | Steven becomes the manual task router. | Operations | Medium | ChatGPT shapes tasks; Codex Orchestrator delegates and consolidates. | Open |
| R-005 | Git integration occurs without proof or crosses unexpected remote history. | Release | High | Allow only proven clean fast-forwards and non-force pushes under standing delegation; interrupt Steven on divergence, secret risk, unexpected files, or unsafe history operations. | Open |
| R-006 | Autonomous UI language implies a candidate is an approved live trade. | Product trust | High | Use candidate, setup, simulation, paper, preview, and live-locked labels until Risk Governor and approvals prove stronger states. | Open |
| R-007 | Broker integration begins before adapter, risk, and audit boundaries exist. | Broker safety | High | Require Broker Adapter, Risk Governor, TradePlan, and Execution Ledger specs before broker implementation. | Open |
| R-008 | Paper and live broker states are blurred. | Broker safety | High | Separate fake, paper, read-only live, preview, and confirmed live adapter modes with visible console labels. | Open |
| R-009 | Manual TradePlan edits bypass risk re-check. | Risk controls | High | Mark edits as manual overrides and require Risk Governor re-check before advancing state. | Open |
| R-010 | Autonomous roadmap expands into protected runtime behavior too early. | Scope control | Medium | Keep ARGUS-A000 docs/config only and require future Goal Charters for implementation. | Open |
| R-011 | A broad frontend rewrite regresses proven trading behavior before the Python boundary is ready. | Architecture | High | Keep Python canonical; use the WPF shell only as a bounded feasibility spike, then define versioned contracts before incremental migration. | Open |
| R-012 | Qt and `app.py` complexity continue growing while the replacement boundary is unclear. | Maintainability | High | Prove the WPF shell and Python contracts first; migrate or retire Qt screens one workflow at a time with test and rollback evidence. | Open |
| R-013 | Frontend modernization improves appearance but weakens safety language. | Product trust | High | Preserve locked/live/paper/simulation labels and screenshot-proof warning states in every UI modernization task. | Open |
| R-014 | Stale branch reports cause Steven or ChatGPT to continue from a superseded branch. | Git / operations | High | Maintain `BRANCH_LEDGER.md`, classify superseded branches, and start new work only from local `master` unless Git Steward says otherwise. | Open |
| R-015 | Duplicate TradePlan/RiskGovernor model paths create conflicting source authority. | Architecture | High | Treat `momentum_hunter/trade_planning.py` and `momentum_hunter/autonomy/*` as canonical; do not merge the older `momentum_hunter/execution/*` branch as-is. | Open |
| R-016 | Review bundles omit imported dependencies or include stale manifest values. | Review quality | Medium | Future bundles must include key imported dependencies such as `trade_planning.py`, `models.py`, `time_utils.py`, and `monitor_targets.py`, while staying curated and excluding secrets/data. | Open |
| R-017 | Simulation engine adapter injection becomes a paper/live transmit path if reused without guards. | Broker safety | High | Before A017/A018, require FakeBroker-only metadata checks and tests proving non-Fake or transmit-capable adapters are rejected before adapter calls. | Open |
| R-018 | Execution Auditor is treated as a hard paper gate before it validates chronology, preview-before-submit, and event consistency. | Broker safety | High | Harden auditor rules and tests before any paper broker skeleton or paper pilot. | Open |
| R-019 | Competing status documents or stale roadmap entries send work down an obsolete architectural path. | Operations | High | Treat `ROADMAP.md` as the sole current-status authority; reconcile it from Git at every substantive task closeout. | Open |
| R-020 | Sparse, stale, or legacy stored chart bars look like current market data or remain mixed with a future actual candle feed. | Product trust | High | Label chart state, source, timeframe, and as-of timestamp; preserve stale candles only with explicit `STALE`; use `UNAVAILABLE` rather than mock, daily, interpolated, or provider fallback. At actual-data cutover, purge legacy JSON hash `DAAC049E4DA87729DE23B312D86B9034FF724F9BF4B2B8ED7FC1AFD293A6AD69`, its 710 mirrored `CRWV` rows, and chart caches before exposing the new feed; prove no mixed source remains. | Mitigated in R011; cutover purge pending |
| R-021 | Schwab's live-only Trader API makes a same-provider automated Paper sequence unavailable and could expose the funded canary account if its data role is confused with execution authority. | Broker safety | Critical | Keep Schwab read-only and bound to the sole expected `2573` `INDIVIDUAL_CASH` account for market/account evidence; retain FakeBroker as the only integrated automated boundary; evaluate Alpaca only through a separately isolated PAPER_ONLY adapter and exact paper-host allowlist. Interrupt on any unexpected account, identity, type, position, permission, funding, live endpoint, or real-order capability. | Mitigated through isolated exact-host Alpaca Paper lifecycle proof; runtime integration and policy gates pending |
| R-022 | A deterministic Shadow fill model overstates edge if treated as broker or exchange truth. | Research validity | High | Retain every rejected/unfilled/partial/ambiguous result, use executable rather than ideal P&L, reconcile selected tickets manually, gate conclusions below 30 completed trades, and label the model as an estimate. | Open; mitigations implemented, sample not started |
| R-023 | Repeated routine approval requests turn Steven into a rubber stamp and obscure the few decisions that change real exposure. | Operations | High | Use standing delegation for proven nonvisual work; reserve Steven interruptions for visual acceptance, anomalies, real orders, destructive actions, credential/provider consequences, paid services, protected semantic expansion, and unsafe Git. | Open |
| R-024 | Correlated trades from one day, sector, catalyst, symbol, or regime masquerade as independent evidence. | Research validity | Critical | Report distinct sessions, regimes, sectors, catalysts, symbols, and time buckets; treat 30 trades as an engineering gate and require at least 10 sessions before strategy review. | Mitigated in SHADOW-006; sector identity remains unavailable |
| R-025 | Unstable report ordering or tie handling changes which candidate is selected without a strategy-version change. | Research validity | Critical | Freeze canonical rank ascending, score descending, stable candidate ID/symbol tie-breaks, the complete ordered candidate list, and every rejection reason. | Mitigated on `master`; operational evidence pending |
| R-026 | Repeated reports select the same symbol or underlying opportunity multiple times and inflate the sample. | Research validity | Critical | Enforce one active global position, one symbol trade per day, no same-day re-entry, deterministic Opportunity ID, source-capture idempotency, and cross-report duplicate suppression. | Mitigated on `master`; operational evidence pending |
| R-027 | Provider schema drift silently changes candidate values, rankings, or eligibility. | Data integrity | Critical | Validate report schema, source provider, source-capture identity, quote symbol/source, required fields, and begin a new sample version after a material source contract change. | Mitigated in SHADOW-006; production provider proof pending |
| R-028 | Provider outages, stale data, machine downtime, or skipped cycles create hidden survivorship and selection bias. | Research validity | Critical | Persist every expected armed in-window five-minute attempt, infer restart gaps as downtime, and preserve completed, skipped, failed, blocked, unfilled, and unavailable outcomes. | Mitigated in SHADOW-006; operational soak pending |
| R-029 | Clock drift, timezone ambiguity, or pipeline latency makes a valid historical setup non-executable by decision time. | Data integrity | Critical | Enforce 30-second quote, 10-minute capture, 5-minute report, and 60-second handoff limits; derive conservative trusted bounds from the exact-host same-response HTTPS clock proof; persist latency evidence and reject future, missing, contradictory, over-age, over-uncertainty, or over-30-second request chronologies. | Mitigated in ARGUS-SHADOW-023 and proven in an isolated live rehearsal; Windows Time readiness and official soak remain pending |
| R-030 | Broad market or sector beta is mistaken for Momentum Hunter ranking edge. | Research validity | High | Record selected, all eligible, deterministic-random, SPY, and IWM baselines; finalize comparable returns at the selected trade exit. | Mitigated in SHADOW-006; sector benchmark deferred |
| R-031 | Fill-model assumptions create most or all observed Shadow profitability. | Research validity | Critical | Reconcile early tickets against manual paperMoney and later canary fills, measure model error, version every assumption change, and never rewrite old samples under a newer model. | Open |
| R-032 | Mid-sample research or threshold changes overfit the active prospective sample. | Research validity | Critical | Hash the constitution and runtime build, enforce both through a write-once arm record and each counted trade, preserve exploratory analysis separately, and require a new sample version for material changes. | Mitigated in SHADOW-006; selector remains unarmed |
| R-033 | Ambiguous broker submission, unsafe retry, partial-fill/cancel races, or restart gaps create duplicate or uncontrolled live orders. | Broker safety | Critical | Require broker order identity, idempotent submit semantics, broker-truth reconciliation, partial-fill/cancel state machines, and fail-closed restart recovery before a plumbing canary. | Mitigated in isolated A003 Paper proof and synthetic restart/partial-fill tests; runtime remains unwired and live transmission unavailable |
| R-034 | Cash settlement or good-faith rules invalidate a low-dollar live canary plan. | Broker safety | Critical | Validate settled cash and account restrictions immediately before every supervised order; keep order size and sequence inside documented cash-account constraints. | Open; pre-canary design required |
| R-035 | A kill switch implemented inside the malfunctioning process cannot stop unsafe execution. | Broker safety | Critical | Require an independently reachable disable/revocation path and prove shutdown plus token-revocation behavior before supervised strategy execution. | Open |
| R-036 | The previously surfaced Schwab Client Secret remains usable without vendor rotation or remediation. | Security | Critical | Permit current accepted-risk read-only use only; prohibit transmitting code until Schwab rotates the secret, permits a replacement app, or explicitly confirms a safe vendor-side remediation. | Open; live-transmit blocker |
| R-037 | The current no-position binding invariant either blocks a legitimate canary fill or is weakened so far that unrelated positions become acceptable. | Broker safety | Critical | Define explicit pre-canary zero-position, canary-active exact-ledger-position, and post-canary zero-position states; any other position remains a hard lockout. | Open; pre-canary design required |
| R-038 | A freshly regenerated monitor report makes copied or stale bid/ask values appear to have a fresh executable quote timestamp. | Data integrity | Critical | Persist monitor-cycle time separately from provider quote timestamp/source; consume exact-host Schwab quote evidence using the oldest bid/ask/quote provider clock; reject missing, future, stale, delayed, closed, extended-hours, mismatched, non-finite, and unrequested evidence; include the source module in the arm build hash; use the dedicated nontransmitting proof CLI to prove one regular-market candidate-plus-SPY/IWM read before arming. | Mitigated on `master`; actual in-market PASS pending |
| R-039 | Format-valid or invented prerequisite hash strings, or an ad hoc arm invocation, enable the automatic selector without proving the underlying safety gates. | Evidence integrity | Critical | Require structured proof artifacts bound to the exact activation, sample, constitution, and runtime build; hash-check referenced evidence; persist canonical paths and digests; revalidate every artifact before runtime accepts the arm; provide one nonmutating check command plus one exact-phrase guarded arm method; and let SHADOW-010 supply that phrase only after synchronized Git, all named tests, retained visual proof, report/capture identity, and schema-v2 live Schwab quote provenance independently pass. | Mitigated on `master`; first automatic in-market run pending |
| R-040 | The only scheduled TradePlan report is already stale when the 9:35 AM ET official entry window opens, or a transient host failure silently loses the day's opening opportunity. | Evidence integrity | Critical | Keep the 7:00 AM premarket capture separate; create one immutable `shadow` capture at 9:35 AM ET per XNYS market-open day; immediately invoke the authenticated guarded Engine Host cycle; derive deterministic command identity from the report hash; persist a write-once accepted-handoff receipt; and retry a complete duplicate report only when its matching receipt is absent. | Mitigated in SHADOW-009; first live market-day run pending |
| R-041 | The automated opening ceremony partially succeeds or restarts between quote proof, bundle finalization, arm persistence, and selector handoff. | Evidence integrity | Critical | Keep each artifact write-once; preflight static/Git evidence before provider access; resume a complete bundle through the same verifier; skip provider work when the arm already revalidates; use report-hash command identity and a write-once handoff receipt; retry only a missing receipt; and reject existing invalid or conflicting proof artifacts without overwriting them. | Mitigated in SHADOW-010; first live market-day run pending |
| R-042 | OAuth refresh or provider latency occurs after the quote proof's evaluation clock, causing valid response timestamps to look future-dated or request duration to be hidden. | Data integrity | Critical | Record request start before token/provider work and evaluate freshness after the response; retain request duration; reject backward and timezone-naive clocks; include the latency test in the immutable freshness proof gate. | Mitigated in SHADOW-011 before first live run |
| R-043 | An opening retry either never occurs or multiplies a single report/cycle/trade. | Operational reliability | Critical | SHADOW-013 replaces scheduler-native restart multiplication with one runner-owned initial attempt plus three retries only for classified infrastructure failures; it reuses stable report/command/cycle identities, preserves complete idempotency, and keeps `IgnoreNew`. | Mitigated in tests; real-session proof pending |
| R-044 | A failed or malformed Engine Host response is persisted as a complete handoff and suppresses a valid retry. | Evidence integrity | Critical | Require an allowlisted semantic terminal outcome plus host instance, process, protocol, command, capture, report, decision-cycle, completion, and optional trade identities; preserve incomplete receipts separately and reject all unknown/failure states. | Mitigated in SHADOW-013 tests; physical failure proof pending |
| R-045 | Selector arming or official selection occurs while the workstation clock is invalid. | Data integrity | Critical | Derive clock proof from the existing exact-host read-only Schwab HTTPS Date response; recompute chronology, skew, and uncertainty; require skew plus uncertainty within five seconds and proof age within five minutes before arm and each decision; use NIST only as an independent drift corroborator and keep Windows Time running with multiple peers. | Candidate-bound live proof and isolated selector rehearsal pass in ARGUS-SHADOW-023; Windows Time is running with a 3-6 ms NIST offset, but settled synchronization status and official opening remain pending |
| R-046 | The 8:50 observer mistakes a still-running task for success/failure or mutates runtime while trying to repair it. | Operational safety | Critical | Keep the heartbeat outside runtime, inspection-only, finite, and tri-state; retire it only after a semantically complete final result and otherwise preserve evidence without retry. | Configured and unit-tested; physical running-task proof pending |
| R-047 | An unrelated outcome updater fails after opening completion and causes Windows to replay the opening ceremony. | Operational reliability | Critical | Give outcome updating its own log/status and preserve completed Shadow opening exit status; executable PowerShell tests prove updater exit `9` cannot create a retryable opening failure. | Mitigated in SHADOW-013 tests; operational proof pending |
| R-048 | A fast active-position loop paints stale, last-price, midpoint, or fabricated P&L as a live result. | Evidence integrity | Critical | Request only the active symbol through the canonical read-only Schwab quote source; freeze a ten-second age threshold; use bid for long and ask for short; persist exact provider/receipt provenance; preserve the last reliable mark but suppress P&L, exits, and live color when stale or halted; reject tampered restart marks. | Mitigated in integrated ARGUS-SHADOW-017 implementation; visual acceptance passed, final-head operational proof pending |
| R-049 | Restart, duplicate quotes, or a mixed sample identity duplicates a position, resets MFE/MAE, reopens a completed trade, or displays obsolete records as current official evidence. | Evidence integrity | Critical | Serialize Shadow state advancement, persist mark/excursion identity, reject replayed/older or duplicate quotes, validate mark direction and bid/ask side on load, keep completed records immutable, and require the current authorized sample identity in WPF active/official lists. | Mitigated in focused tests; operational restart proof pending |
| R-051 | A valid authenticated Engine Host result exceeds the request-sized client limit after the host has already persisted a decision or FakeBroker state, leaving the caller without a terminal handoff. | Operational reliability | Critical | Keep requests capped at 64 KiB, give authenticated loopback responses a separate bounded 1 MiB limit, reject oversized lines even when the newline arrives in the final chunk, and preserve report-hash idempotency so a missing receipt can be completed without a second decision or trade. | Mitigated in ARGUS-SHADOW-023; isolated terminal replay proved one decision, one trade, and one handoff |
| R-050 | An interactive Windows task or desktop-only Codex observer misses a market gate after reboot or when no user session exists. | Operational reliability | Critical | Replace interactive launch authority with `MomentumHunterAutomation`, immediate automatic startup, finite process recovery, a SYSTEM-owned 08:15 wake-only task, persisted one-time receipts, fail-closed missed-window handling, and downstream-only ephemeral read-only Codex review. Do not configure interactive autologon. Require a nonmarket service-account canary and reboot-without-login proof before another prospective opening; use BIOS RTC/restore-on-AC-loss for fully powered-off recovery. | Service, wake task, nonmarket canary, Codex probe, and time sync pass; reboot-without-login proof remains open |
| R-052 | Running the unattended service under Steven's interactive account couples market readiness to a forgotten or changed Windows password, while moving current-user DPAPI material to another identity could silently broaden secret access. | Credential security | Critical | Do not reset the password, copy plaintext, or change DPAPI scope implicitly. Preserve the existing identity while DPAPI compatibility is required; if a future service identity is introduced, require a separately encrypted service envelope, exact service-SID ACLs, immutable account binding, migration nonmutation, revocation isolation, and no Codex dependency. | Existing password recovered locally without reset or disclosure; service canary proved current DPAPI/account binding, but future password changes remain an operational risk |
| R-053 | A read-only observer, antivirus scanner, or another process briefly locks the supervisor state receipt and crashes the scheduling loop during a market window. | Operational reliability | Critical | Keep write-once atomic replacement, retry only Windows access-denied/sharing-violation replacement failures for a bounded one-second budget, preserve the prior valid receipt, and retain wrapper restart recovery for persistent failure. | Mitigated on canonical `master` at `252cdc7`; installed lock-contention proof passed without a restart or Application error |
| R-054 | An offline review packet leaks credentials/account identity, mixes event chains, overwrites evidence, or presents a derivation as a stored trading fact. | Evidence integrity / security | Critical | Require explicit files, exact hashes, current activation/policy validation, terminal lifecycle and cross-record identity checks, field classification, allowlisted evidence, caller-supplied known-sensitive scanning, write-once pair creation, source nonmutation checks, and no network/runtime capability. | Mitigated in ARGUS-SHADOW-024; post-Monday reconciliation verification pending |
| R-055 | A successful fallback value inherits the warning or trust state of a different provider attempt, or an unrelated article is presented as direct candidate news. | Data integrity | Critical | Preserve provenance per displayed field, preserve provider attempts separately, mark all current enrichment research-only/execution-ineligible, require explicit issuer/company evidence for direct catalyst attribution, and block evidence-review score authority when the relationship is unresolved. | Mitigated on `master` by ARGUS-DATA-001 `488cbca`; prospective production evidence pending |
| R-056 | Evidence labeled unresolved or research-only continues to influence score, scenario rank, TradePlan readiness, or automatic selection. | Data integrity / execution safety | Critical | Version and fingerprint the prospective composite configuration; force blocked catalyst confidence and cluster bonuses to zero; gate research-only price evidence; and make Shadow selection reject missing, legacy, or contradictory authority records before eligibility. Preserve old reports without recomputation. | Mitigated on `master` by ARGUS-DATA-001B `fe8c929`; prospective production evidence pending |
| R-057 | Opening relative volume compares partial-session volume with a full-day historical average and overstates or understates momentum authority. | Data integrity / strategy validity | Critical | Use versioned time-normalized RVOL from terminal canonical Schwab minute bars, identical elapsed-session windows, explicit source/symbol/session-minute/baseline identity, five-session minimum, and independent selector revalidation. Preserve pre-DATA-002 reports and keep every legacy ratio research-only. | Mitigated by DATA-002; prospective operational sufficiency remains observable and fail-closed |
| R-058 | A near-live `CHART_EQUITY` bar is treated as final or canonical even though Schwab provides no observed completion marker and its stream volume can differ from `/pricehistory`. | Data integrity | Critical | Preserve every stream version as unreconciled evidence; key by source/symbol/session/timestamp; reconcile completed minutes against `/pricehistory`; preserve both versions and corrections; show gaps/stale state; and prohibit WPF or execution authority until R032 proves persistence and recovery. | Mitigated in R032 storage and reconciliation; consumer activation remains gated by R033 |
| R-059 | An unsupported Yahoo quote request or a research-only monitor refresh is mistaken for executable last/bid/ask evidence. | Data integrity / execution safety | Critical | Use one exact-host Schwab quote batch; require matching symbol/source, real-time regular-session tradability, fresh provider timestamps, finite last/bid/ask/spread, and HTTPS clock proof; never let Nasdaq/Yahoo chart fallback inherit authority; reapply plan and catalyst gates after monitor refresh; preserve failures explicitly. | Mitigated by DATA-001C implementation `17e5b50`; first prospective opening observation pending |
| R-060 | A transport proof with one or two bars is presented as a usable workstation chart, or missing Daily history is mistaken for a renderer defect. | Data integrity / operator trust | High | Separate live transport proof from chart-depth acceptance; backfill bounded Schwab one-minute and daily `/pricehistory`; require minimum row depth; retain source/timestamps/corrections; fail visual acceptance when depth is insufficient; never fill gaps with legacy, Yahoo, quote-derived, simulated, or cross-timeframe candles. | Mitigated by R032B live proof, Steven-accepted R033 dense chart, R032C synthetic transition, and installed uncached-QQQ proof with 180 bars on all four intervals |
| R-061 | A live-chart UI silently substitutes legacy, mock, quote-derived, or cross-timeframe data when a Schwab partition is absent or incomplete. | Product trust / data integrity | Critical | Keep intraday reads exclusive to the R032 store; bind Daily only to the separate R032B Schwab daily store; derive 5m/15m only from stored 1m bars; expose missing, stale, gap, correction, and unreconciled states; return `UNAVAILABLE` or `PARTIAL` instead of substituting another source. | Mitigated on canonical `master` at `af783da`; focused tamper/legacy tests and actual-store proof pass |
| R-062 | A newly selected or newly ranked symbol shows an empty chart indefinitely because historical backfill remains a manual command. | Product readiness / provider operations | High | R032C must inspect cache first, coalesce one bounded background request per symbol, enforce universe/rate/retry limits, expose loading/failure state, and keep WPF provider-free. | Mitigated: implementation, visual acceptance, integration, installed host reload, and uncached-QQQ proof all pass |
| R-063 | Destructive legacy-candle cutover breaks outcome/research/evidence paths or allows the legacy cache to be recreated after deletion. | Data integrity / migration safety | Critical | Complete R034A first: migrate or explicitly retire every active reader/writer, add a plan-only reference/hash/row verifier, prove absent-file behavior and no recreation, preserve historical outputs, and keep the separate Daily research dataset outside the deletion scope. Require Steven approval only after the verifier names the exact CRWV JSON and 710 SQLite rows. | R034A is integrated and installed-runtime proven; risk remains open only for the separately approved R034 archive/deletion |
| R-064 | A stock already above resistance is treated as a fresh breakout and the entry is silently chased above current price, creating a plan that never existed at the original decision level. | Data integrity / execution safety | Critical | Preserve the completed-Daily breakout level, classify above-level snapshots as `RECLAIM_REQUIRED`, require a later pullback-and-recross chronology, fingerprint the identity, preserve the blocker through monitor refresh, and make Shadow independently reject missing or contradictory setup evidence. | Mitigated by DATA-003; actual reclaim observation remains DATA-004 work |
| R-065 | The successful opening-capture program makes TradePlan semantics morning-only, or an expired/missed setup is silently relabeled as a later reclaim and regains authority. | Data integrity / execution safety | Critical | Use one prospective same-session `INTRADAY` contract for opening, continuation, pullback, reclaim, and supported catalyst setups; bind setup-aware windows, forced flat, lifecycle, source evidence, predecessor identity, plan ID, and fingerprint; make terminal states immutable; and require every replacement to be a new successor plan that Risk Governor and Shadow independently revalidate at decision time. | Mitigated by DATA-004; continuous later-session setup production remains future work |
| R-066 | Reference capital, hidden defaults, stale account evidence, or a mismatched account/portfolio state creates an unsafe simulated or future executable quantity. | Account allocation / execution safety | Critical | Prohibit executable use of `$500` reference sizing; require explicit versioned policy, fresh authoritative account/portfolio evidence, cash/risk/notional/concurrency/daily-loss limits, transmission state, and frozen auditable fingerprints. Preserve `idealRiskQuantity`, `providerExecutableQuantity`, and `finalAuthorizedQuantity`; unknown provider capability or malformed evidence blocks before order creation. | DATA-005/DATA-005A complete; DATA-005B provider-neutral precursor active; numeric canary and strategy-research policies intentionally unfrozen |
| R-067 | A stale Engine Host supervisor repeatedly replaces current idle hosts because its in-memory runtime-hash contract predates the installed code, leaving Engine Host unavailable even though opening capture remains independent. | Operational reliability / runtime identity | High | Include every allocation-boundary module in runtime identity; after the synchronized DATA-005A release, perform one controlled service refresh, terminate only authenticated idle orphan hosts, preserve any active cycle, and prove exactly one current healthy host owns the endpoint. | Mitigated after `dff993c`: elevated refresh changed the service PID and authenticated proof shows one matching endpoint/lock/process/listener, current runtime hash, Healthy state, and no active cycle |
| R-068 | A Paper configuration, credential, or adapter accidentally reaches an Alpaca live endpoint or makes live capability reachable through a mode toggle. | Broker safety / credential security | Critical | Use an exact PAPER_ONLY host allowlist, separate lane-bound local Paper credentials, structural rejection of every live host, no shared live adapter toggle, secret scans, and negative endpoint-isolation tests. Funding, money movement, and all live Alpaca work require a separate Steven authorization. | Mitigated in the isolated feature stack: Canary authentication and fractional limit/cancel proof passed on the exact Paper host; live host and research lane are structurally rejected; no runtime integration exists. Integration and future lifecycle slices must preserve these gates. |
| R-069 | Whole-share quantization, a one-position canary limit, a tiny account balance, or independently reusing the same account budget for several candidates becomes an accidental strategy law or overstates executable participation. | Strategy validity / allocation | Critical | Keep broker capability, account policy, and strategy intent separate; preserve the three quantity stages; version concurrency and rank-participation policies; retain every independently eligible candidate; consume one shared notional/open-risk budget across rank-ordered admissions; and report canary-realistic and strategy-research lanes separately without retrospective trades. | Mitigated on canonical `master` by DATA-005B contracts with explicit lane and lineage guards; numeric policies and prospective runtime evidence remain pending |
| R-070 | Optimistic Alpaca Paper fills or mixed canary/research samples overstate executable edge or silently overwrite conservative Momentum Hunter evidence. | Research validity / evidence integrity | Critical | Preserve `ALPACA_PAPER_EXECUTION_RESULT` separately from `MH_CONSERVATIVE_EXECUTABLE_RESULT`; version provider, fill, capital, risk, concurrency, and rank policies; preserve unfilled/partial/stale/gap outcomes; and start a new prospective identity after every material change. | Mitigated on canonical `master` by offline dual-result contracts; account-lane mismatches fail closed, no Alpaca Paper sample has begun, and runtime integration remains pending |
| R-071 | Provider-generated credentials are exposed through chat/command history or Windows hidden-console paste stores a control character instead of the clipboard value. | Credential security / operator workflow | Critical | Never accept credentials through chat or arguments; rotate any exposed pair; use a local masked Paste-button dialog; reject whitespace/control characters; encrypt each lane under distinct CurrentUser DPAPI entropy; clear matching credential clipboard content after storage; and prove authentication with a redacted one-request canary. | Mitigated for Canary onboarding; first pair rotated, regression test added, research slot remains empty |
| R-072 | A successful nonmarketable fractional limit/cancel probe is mistaken for proof of market fills, protective stops, advanced orders, exact liquidation, or production readiness. | Broker safety / evidence integrity | Critical | Track every capability independently as `PROVEN`, `DOCUMENTED_UNPROVEN`, `UNSUPPORTED`, or `UNKNOWN`; bind direct evidence to implementation and provider fingerprints; keep unknown capability fail-closed; and require separate market-hours lifecycle proof before allocator integration or a Paper sample. | A003 directly proves bounded Paper market entry, fractional stop/stop-limit, target replacement/cancel, and exact liquidation. Partial-fill/restart remain synthetic; advanced/streaming/linked-protection capabilities remain unproven and blocked. |
| R-073 | A lost provider response, process restart, or partial fill causes a duplicate Paper command, an orphan protective order, or a residual Canary position. | Broker safety / lifecycle integrity | Critical | Write and hash the plan before mutation; freeze unique client-order IDs; query before every submit/replace; recover once after ambiguous responses without blind retry; reject contradictory/orphan/foreign state; preserve partial fills; cap flattening at three exact-quantity IDs; and require zero final positions/orders. | A003 direct lifecycle ended with independently verified zero positions/orders; ARGUS-BROKER-ALPACA-004 adds durable entry-intent recovery, recovered stop installation, emergency-flat reconciliation, and concurrent-manager exclusion. Prospective strategy-generated evidence remains pending. |
| R-074 | A slow opening capture, service restart, stale Git pin, or duplicate lifecycle manager starts a Paper entry late or leaves a filled Paper position unmanaged. | Broker safety / operational reliability | Critical | Make Paper depend on the terminal same-date opening capture; refresh the clock after capture; enforce a 15-minute start window and exact Git head; persist intent before submission; never submit a late entry during recovery; use a Windows-wide lifecycle mutex; host a broker-resident stop; require exact-flat emergency exits; resume only owned client-order identities; and repin Paper with its opening dependency. | Mitigated in ARGUS-BROKER-ALPACA-004 by focused restart/timing/partial-exit tests and full regression; installed-service and first prospective provider observation remain pending. |
| R-075 | Correlated indicators, lookahead, gap reuse, or rewritten setup identity makes sequential breakout research look more predictive than it was prospectively. | Research validity / evidence integrity | Critical | BREAKOUT-001 uses only prior completed windows, preserves versioned policy/source/event fingerprints, resets on gaps, gives pullback/reclaim new MONITOR setup identities with validated predecessor chains, rejects tampering/conflicts, and remains dormant/research-only. Require a frozen sufficient prospective cohort before BREAKOUT-002 conclusions or any authority proposal. | Mitigated in isolated BREAKOUT-001 implementation; current-master integration and a sufficient prospective cohort remain pending |
| R-077 | Partial horizons, missing bars, historical replay, later corrections, or hidden unavailable events inflate sequential-breakout outcome claims or rewrite prior evidence. | Research validity / evidence integrity | Critical | Bind outcomes to immutable BREAKOUT-001 event-chain prefixes; require exact completed same-session bars; preserve pending, gap, missing, correction, and unavailable states; make revisions append-only; keep historical and prospective denominators separate; and prohibit conclusion authority in the synthetic contract. | Mitigated in BREAKOUT-002A with 29 focused tests; real calibration remains gated on a separately frozen sufficient prospective cohort |

## Protected Areas
Protected areas require exact task scope and Hard Chew proof. Interrupt Steven before semantic expansion, destructive migration, secret exposure/revocation, real execution, or unexpected external state.
