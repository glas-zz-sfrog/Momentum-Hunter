# ARGUS-CONTINUOUS-001 Goal Charter

## Goal

Reconcile the proven candidate lifecycle, regime, event, catalyst, and
sequential-breakout evidence contracts onto current canonical `master` without
changing or installing the runtime pinned for Tuesday's opening and Paper jobs.

## Operator Value

Prepare one coherent continuous-intraday evidence foundation that can later
support prospective candidate reevaluation and plan generation without
confusing branch age, provider proof, or dormant research models with runtime
authority.

## Scope

- Reapply only proven code/test commits in dependency order.
- Resolve stale governance appendices against current Roadmap truth.
- Reverify the combined stack on the current Alpaca Paper engineering base.
- Keep every module dormant until a separately verified integration/install
  window.

## Exclusions

- No service, Engine Host, scheduler, WPF, capture, or provider wiring.
- No score, readiness, alert, selection, TradePlan, Risk Governor, allocation,
  Shadow, or broker/order semantic change.
- No production persistence, account query, provider call, credential access,
  Paper mutation, or real order.
- No merge to `master` before Tuesday's terminal opening/Paper evidence.

## Acceptance

- Current-master worktree is isolated from the canonical checkout.
- Five implementation commits are reconciled in dependency order.
- Compileall, focused tests, adjacent regressions, and full Python discovery
  pass.
- Static scans prove no active runtime import, network/broker capability,
  secret, or protected-path expansion.
- Branch-local Roadmap says `IMPLEMENTED_PENDING_MERGE`.
- Canonical `master`, installed runtime, and scheduled jobs remain unchanged.
