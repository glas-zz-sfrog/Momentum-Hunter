# ARGUS-BREAKOUT-002A Goal Charter

## Goal

Build the roadmap-authorized offline mechanics for attaching deterministic,
append-only outcome evidence to immutable BREAKOUT-001 events without claiming
that a real prospective cohort is calibrated or predictive.

## Operator Outcome

Steven can later inspect what happened 5, 15, 30, and 60 minutes after each
breakout or reclaim event, including unavailable evidence, without hindsight
rewriting, hidden denominators, or historical replay being counted as a live
prospective sample.

## Scope

- Bind each outcome to the exact BREAKOUT-001 event-chain prefix and policy.
- Measure forward return, MFE, MAE, trigger hold/failure, and first failure only
  from exact completed same-session canonical bars.
- Preserve pending, gap, missing, corrected, and session-unavailable states.
- Maintain append-only correction/revision lineage and explicit-path atomic
  storage.
- Produce descriptive cohort summaries split by prospective versus historical
  observation mode.
- Keep cohort threshold unset by default and conclusion authority permanently
  false in this precursor.

## Non-Goals

- No provider, account, network, broker, order, service, scheduler, Engine Host,
  WPF, Shadow, production-store, or generated-report integration.
- No score, rank, readiness, alert, TradePlan, Risk Governor, allocation,
  selector, or execution authority.
- No BREAKOUT-002 edge conclusion, threshold tuning, recommendation, or
  production activation.
- No retrospective trade or sample claim.

## Acceptance Evidence

- Compileall passes.
- All 32 focused outcome tests pass.
- All 136 adjacent evidence/candle/plan tests pass.
- Full Python discovery passes 1,651 tests.
- Exact-horizon, no-future-bar, correction, gap, session-boundary, source
  nonmutation, observation-mode binding, monotonic revision chronology, tamper,
  idempotency, cohort-denominator, and authority-negative tests pass.
- Static scans prove no runtime importer, provider/broker capability, or
  credential-shaped value.
- Canonical `master`, installed runtime, Tuesday jobs, and provider/order state
  remain unchanged.

## Classification

`IMPLEMENTED_PENDING_MERGE` on
`codex/ARGUS-BREAKOUT-002A-synthetic-outcome-contract`. Integration waits for
Tuesday's terminal evidence and current-base reconciliation after the common
ARGUS-CONTINUOUS-001 branch. BREAKOUT-002 conclusions remain
`WAITING_DEPENDENCY` on a separately frozen sufficient prospective cohort.
