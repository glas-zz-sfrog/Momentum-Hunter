# ARGUS-AFTER-CLOSE-002 Goal Charter

## Goal

Prove how far the current decision chain can consume real preserved canonical
Schwab regular-session evidence without creating retrospective authority or
touching any provider, broker, official sample, runtime, or production store.

## Acceptance

- Bind every source by path, SHA-256, and internal evidence identity.
- Preserve original market time separately from replay evaluation time.
- Run DATA-004, Paper Risk, DATA-005B allocation, order-intent, protection, and
  serialization contracts only as far as real evidence legitimately permits.
- Stop at a real gate rather than modifying evidence to manufacture a trade.
- Prove downstream contracts separately with clearly identified synthetic data.
- Preserve all sources byte-for-byte and make zero network/provider calls.
- Pass focused, adjacent, and full bounded Python verification.

## Protected Boundaries

Canonical master, installed automation, scheduler, service, opening evidence,
Paper/Shadow state, credentials, account data, broker endpoints, and production
candle stores are read-only or entirely absent from this task.

## Result

`IMPLEMENTED_PENDING_MERGE_AFTER_AUG13_OPENING`. The real replay reached
DATA-004 and stopped at `PAPER_ENTRY_TRIGGER_NOT_REACHED`; the full real chain
therefore remains unproven. Synthetic tests separately prove the downstream
zero-call contract path.
